
    <body>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Контакты — расчёт зеркала за 5 минут</title>
  <meta name="description" content="Оставьте заявку на расчет зеркала. Ответим быстро. Телефон, мессенджеры, адрес." />
  <link rel="stylesheet" href="css/styles.css" />
</head>
  <header class="header" data-header>
    <a class="logo" href="index.html" aria-label="На главную">
      <span class="logo__mark"></span> MIRR.
    </a>

    <nav class="nav" aria-label="Основная навигация">
      <a class="nav__link" href="index.html">Основная</a>
      <a class="nav__link" href="catalog.html">Каталог</a>
      <a class="nav__link is-active" href="contacts.html">Контакты</a>
    </nav>

    <div class="header__actions">
      <a class="btn btn--primary" href="#form">Получить расчёт</a>
      <button class="burger" data-burger aria-label="Открыть меню">
        <span></span><span></span>
      </button>
    </div>
  </header>

  <main class="page">
    <section class="section">
      <div class="container">
        <div class="section__head reveal">
          <h1 class="h1">Контакты</h1>
          <p class="muted">Напишите/позвоните или оставьте заявку — ответим максимально быстро.</p>
        </div>

        <div class="contact-grid">
          <div class="card reveal">
            <h2 class="h3">Связь</h2>
            <p class="muted">Телефон / мессенджеры</p>

            <div class="card reveal">
  <h2 class="h3">Связь</h2>
  <p class="muted">Свяжитесь с нами удобным способом</p>

  <div class="contact-list">
    <a class="contact-item" href="tel:+79991234567">
      <span>Телефон</span>
      <strong>+7 999 123-45-67</strong>
    </a>

    <a class="contact-item"
       href="https://t.me/your_username"
       target="_blank" rel="noopener">
      <span>Telegram</span>
      <strong>Написать</strong>
    </a>

    <a class="contact-item" href="mailto:info@yourdomain.ru">
      <span>Email</span>
      <strong>info@yourdomain.ru</strong>
    </a>
  </div>

  <div class="divider"></div>
  <p class="fineprint">Работаем по России. Отвечаем обычно в течение 15 минут.</p>
</div>


            <div class="divider"></div>
            <p class="fineprint">Подставь реальные ссылки на мессенджеры и почту.</p>
          </div>

          <div class="card reveal" style="transition-delay:120ms" id="form">
            <h2 class="h3">Быстрый расчёт</h2>
            <p class="muted">Заполните 3 поля — этого достаточно, чтобы назвать цену.</p>

            <form class="form" data-form>
              <label class="field">
                <span>Имя</span>
                <input name="name" type="text" placeholder="Ваше имя" required />
              </label>

              <label class="field">
                <span>Телефон</span>
                <input name="phone" type="tel" placeholder="+7 ___ ___-__-__" required />
              </label>

              <label class="field">
                <span>Что нужно?</span>
                <select name="type" required>
  <option value="" disabled selected>Что нужно?</option>
  <option>Зеркало</option>
  <option>Полка</option>
  <option>Панно</option>
  <option>Нестандартное изделие</option>
  <option>Нужна консультация</option>
</select>

              </label>

              <label class="field">
                <span>Комментарий (необязательно)</span>
                <textarea name="comment" rows="3" placeholder="Размеры, фото стены, сроки"></textarea>
              </label>

              <button class="btn btn--primary btn--lg" type="submit">Получить расчёт</button>
              <p class="fineprint">Мы не спамим. Связь — только по вашему запросу.</p>
            </form>
          </div>
        </div>

        <div class="map card reveal" style="margin-top:16px">
          <div class="map__head">
            <h2 class="h3">Адрес</h2>
            <iframe
  src="https://yandex.ru/map-widget/v1/?um=constructor%3AВАШ_ID_КАРТЫ"
  width="100%"
  height="280"
  frameborder="0"
  style="border-radius:22px;"
  loading="lazy">
</iframe>

          </div>
        </div>
      </div>
    </section>
  </main>

  <footer class="footer">
    <div class="container footer__row">
      <div class="footer__left">
        <div class="logo logo--small"><span class="logo__mark"></span> MIRR.</div>
        <p class="muted">Зеркала на заказ • Подсветка • Монтаж</p>
      </div>
      <div class="footer__right">
        <a class="footer__link" href="catalog.html">Каталог</a>
        <a class="footer__link" href="#form">Рассчёт</a>
      </div>
    </div>
  </footer>
<style>
:root{
  --bg:#fbfbfc;
  --panel:#ffffff;
  --soft:#f4f2ef;
  --text:#121418;
  --muted:#6b7078;
  --line:rgba(18,20,24,.10);

  --primary:#1a1f2a;
  --primary2:#2a3445;

  --radius:22px;
  --shadow: 0 20px 60px rgba(18,20,24,.08);
}

*{box-sizing:border-box}
html,body{height:100%}
body{
   margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans", "Helvetica Neue", sans-serif;
  color:var(--text);
  background: linear-gradient(
    120deg,
    #f2f1ff,
    #ecfbff,
    #fdf7f0,
    #eef2ff
  );
  background-size: 1000% 1000%;
  animation: gradientFlow 18s ease infinite;
  line-height:1.35;
}
body::before{
  content:"";
  position:fixed;
  inset:-20%;
  z-index:-1;
  background: radial-gradient(600px 600px at 20% 20%, rgba(180,190,255,.25), transparent 60%),
    radial-gradient(700px 700px at 80% 30%, rgba(180,230,255,.22), transparent 65%),
    radial-gradient(500px 500px at 50% 80%, rgba(255,220,200,.22), transparent 60%);
  filter: blur(80px);
  animation: floatGlow 24s ease-in-out infinite alternate;
}

img{max-width:100%; display:block}
a{color:inherit; text-decoration:none}
button{font:inherit}
.container{width:min(1100px, calc(100% - 40px)); margin:0 auto}

.h1{font-size: clamp(34px, 5vw, 56px); letter-spacing:-.03em; margin:0 0 10px;line-height: 1.2}
.h2{font-size: clamp(26px, 3.5vw, 34px); letter-spacing:-.02em; margin:0;line-height: 1.2}
.h3{font-size: 18px; margin:0 0 6px;line-height: 1.2}
.lead{font-size: 16px; color:var(--muted); margin:0 0 18px; max-width: 52ch;line-height: 1.2}
.muted{color:var(--muted);line-height: 1.2}
.fineprint{font-size:12px; color:var(--muted); margin:10px 0 0;line-height: 1.2}

.badge{
  display:inline-flex; gap:10px;
  padding:10px 14px;
  border:1px solid var(--line);
  border-radius:999px;
  background: rgba(255,255,255,.55);
  backdrop-filter: blur(10px);
  box-shadow: 0 10px 30px rgba(18,20,24,.05);
  margin-bottom: 14px;
}

.header{
  position:sticky; top:0; z-index:50;
  background: rgba(251,251,252,.65);
  backdrop-filter: blur(14px);
  border-bottom:1px solid var(--line);
}
.header{
  display:flex; align-items:center; justify-content:space-between;
  padding:14px 20px;
}
.logo{display:flex; align-items:center; gap:10px; font-weight:700; letter-spacing:.04em}
.logo__mark{
  width:22px; height:22px; border-radius:8px;
  background: linear-gradient(135deg, rgba(18,20,24,.08), rgba(18,20,24,.18));
  border:1px solid var(--line);
}
.logo--small{opacity:.9}

.nav{display:flex; gap:18px}
.nav__link{
  padding:10px 10px;
  border-radius: 12px;
  color: var(--muted);
  transition: transform .2s ease, background .2s ease, color .2s ease;
}
.nav__link:hover{background: rgba(18,20,24,.04); color: var(--text); transform: translateY(-1px)}
.nav__link.is-active{color:var(--text); background: rgba(18,20,24,.05)}

.header__actions{display:flex; gap:10px; align-items:center}

.btn{
  display:inline-flex; align-items:center; justify-content:center;
  gap:10px;
  padding:11px 14px;
  border-radius: 16px;
  border:1px solid transparent;
  cursor:pointer;
  transition: transform .2s ease, box-shadow .2s ease, background .2s ease, border-color .2s ease;
  user-select:none;
}
.btn--lg{padding:13px 16px; border-radius:18px}
.btn--primary{
  background: linear-gradient(180deg, var(--primary), var(--primary2));
  color:#fff;
  box-shadow: 0 18px 45px rgba(18,20,24,.18);
}
.btn--primary:hover{transform: translateY(-1px); box-shadow: 0 22px 58px rgba(18,20,24,.22)}
.btn--ghost{
  background: rgba(255,255,255,.55);
  border-color: var(--line);
  backdrop-filter: blur(10px);
}
.btn--ghost:hover{transform: translateY(-1px); background: rgba(255,255,255,.72)}

.burger{
  display:none;
  width:44px; height:44px;
  border-radius:16px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.55);
  backdrop-filter: blur(10px);
}
.burger span{
  display:block; width:18px; height:2px; background: var(--text);
  margin:0 auto 5px;
}
.burger span:last-child{margin-bottom:0}

.hero{padding: 44px 0 18px}
.hero__grid{
  display:grid; gap:28px;
  grid-template-columns: 1.05fr .95fr;
  align-items:center;
}
.hero__cta{display:flex; gap:12px; flex-wrap:wrap; margin: 10px 0 14px}

.glass-card{
  background: rgba(255,255,255,.55);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  overflow:hidden;
  box-shadow: var(--shadow);
  backdrop-filter: blur(12px);
  transform: translateZ(0);
}
.glass-card img{aspect-ratio: 4/4; object-fit: cover}
.glass-card__hint{
  display:flex; gap:10px; flex-wrap:wrap;
  padding:14px;
  border-top:1px solid var(--line);
}
.glass-card__hint span{
  font-size:12px;
  padding:7px 10px;
  border-radius:999px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.6);
}

.trust{
  display:flex; gap:14px; flex-wrap:wrap;
  margin-top:10px;
}
.trust__item{
  padding:12px 14px;
  border:1px solid var(--line);
  border-radius: 18px;
  background: rgba(255,255,255,.55);
  backdrop-filter: blur(10px);
}
.trust__num{font-weight:700; display:block}
.trust__txt{font-size:12px; color:var(--muted)}

.section{padding: 42px 0}
.section--soft{background: linear-gradient(180deg, transparent, rgba(244,242,239,.65), transparent)}
.section__head{margin-bottom: 16px}

.cards{
  display:grid;
  grid-template-columns: repeat(4, 1fr);
  gap:12px;
}
.card{
  padding:16px;
  border-radius: var(--radius);
  border:1px solid var(--line);
  background: rgba(255,255,255,.62);
  backdrop-filter: blur(12px);
  box-shadow: 0 10px 40px rgba(18,20,24,.06);
}

.split{
  display:grid;
  grid-template-columns: 1.05fr .95fr;
  gap:16px;
  align-items:start;
}
.steps{margin:14px 0 0; padding:0; list-style:none; display:grid; gap:10px}
.steps li{
  display:flex; gap:12px; align-items:center;
  padding:12px 14px;
  border-radius:18px;
  background: rgba(255,255,255,.55);
  border:1px solid var(--line);
}
.steps span{
  width:34px; height:34px; border-radius:14px;
  display:grid; place-items:center;
  background: rgba(18,20,24,.05);
  border:1px solid var(--line);
  font-weight:700;
}

.quote{
  padding:16px;
  border-radius: var(--radius);
  border:1px solid var(--line);
  background: rgba(255,255,255,.62);
  backdrop-filter: blur(12px);
  box-shadow: var(--shadow);
}
.quote__text{margin:0 0 8px; font-size:16px}
.quote__meta{margin:0 0 16px; color:var(--muted); font-size:13px}

.form{display:grid; gap:12px; margin-top:12px}
.field span{display:block; font-size:12px; color:var(--muted); margin:0 0 6px}
input, select, textarea{
  width:100%;
  padding:12px 12px;
  border-radius: 16px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.75);
  outline:none;
  transition: box-shadow .2s ease, border-color .2s ease, transform .2s ease;
}
input:focus, select:focus, textarea:focus{
  border-color: rgba(26,31,42,.35);
  box-shadow: 0 0 0 4px rgba(26,31,42,.08);
}

.grid4{
  display:grid;
  grid-template-columns: repeat(4, 1fr);
  gap:12px;
}
.tile{
  border-radius: var(--radius);
  overflow:hidden;
  border:1px solid var(--line);
  background: rgba(255,255,255,.60);
  box-shadow: 0 12px 42px rgba(18,20,24,.07);
  transition: transform .25s ease, box-shadow .25s ease;
}
.tile:hover{transform: translateY(-3px); box-shadow: 0 20px 60px rgba(18,20,24,.12)}
.tile img{aspect-ratio: 4/3; object-fit:cover}
.tile__meta{padding:14px; display:flex; justify-content:space-between; gap:10px}
.tile__meta span{color:var(--muted); font-size:12px}

.center{display:flex; justify-content:center}

.footer{
  padding: 26px 0 34px;
  border-top:1px solid var(--line);
  background: rgba(251,251,252,.70);
  backdrop-filter: blur(14px);
}
.footer__row{display:flex; justify-content:space-between; align-items:center; gap:14px}
.footer__right{display:flex; gap:14px}
.footer__link{color:var(--muted)}
.footer__link:hover{color:var(--text)}

.page{padding-top: 8px}

.filters{display:flex; gap:10px; flex-wrap:wrap; margin-bottom: 14px}
.chip{
  padding:10px 12px;
  border-radius:999px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.55);
  cursor:pointer;
  color:var(--muted);
  transition: transform .2s ease, background .2s ease, color .2s ease;
}
.chip:hover{transform: translateY(-1px); color: var(--text)}
.chip.is-active{color:var(--text); background: rgba(18,20,24,.06)}

.catalog{
  display:grid;
  grid-template-columns: repeat(4, 1fr);
  gap:12px;
}
.product__inner{
  width:100%;
  padding:0;
  border:1px solid var(--line);
  border-radius: var(--radius);
  background: rgba(255,255,255,.60);
  overflow:hidden;
  cursor:pointer;
  box-shadow: 0 12px 42px rgba(18,20,24,.07);
  transition: transform .25s ease, box-shadow .25s ease;
}
.product__inner:hover{transform: translateY(-3px); box-shadow: 0 20px 60px rgba(18,20,24,.12)}
.product__inner img{aspect-ratio: 4/3; object-fit: cover}
.product__meta{padding:14px}
.product__row{display:flex; justify-content:space-between; align-items:center; margin-top:10px}
.pill{
  font-size:12px;
  padding:6px 10px;
  border-radius:999px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.65);
}
.price{font-weight:700}

.cta-banner{
  margin-top: 16px;
  display:flex; justify-content:space-between; align-items:center; gap:12px;
  padding:16px;
  border-radius: var(--radius);
  border:1px solid var(--line);
  background: linear-gradient(135deg, rgba(255,255,255,.70), rgba(244,242,239,.70));
  backdrop-filter: blur(12px);
}

.contact-grid{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap:12px;
}
.contact-list{display:grid; gap:10px; margin-top:12px}
.contact-item{
  display:flex; justify-content:space-between; gap:10px;
  padding:12px 14px;
  border:1px solid var(--line);
  border-radius:18px;
  background: rgba(255,255,255,.55);
}
.contact-item span{color:var(--muted); font-size:12px}
.divider{height:1px; background: var(--line); margin:14px 0}

.map__box{
  height:280px;
  border-radius: var(--radius);
  border:1px dashed rgba(18,20,24,.18);
  display:grid; place-items:center;
  background: rgba(255,255,255,.45);
}

/* Floating CTA */
.floating-cta{
  position: fixed;
  right: 18px;
  bottom: 18px;
  z-index: 60;
  opacity: 0;
  transform: translateY(10px);
  pointer-events:none;
  transition: opacity .25s ease, transform .25s ease;
}
.floating-cta.is-visible{
  opacity:1;
  transform: translateY(0);
  pointer-events:auto;
}

/* Modal */
.modal{
  position:fixed; inset:0; z-index:80;
  display:none;
}
.modal.is-open{display:block}
.modal__overlay{
  position:absolute; inset:0;
  background: rgba(18,20,24,.40);
  backdrop-filter: blur(6px);
}
.modal__dialog{
  position:relative;
  width:min(820px, calc(100% - 28px));
  margin: 60px auto;
  padding:18px;
  border-radius: var(--radius);
  border:1px solid rgba(255,255,255,.20);
  background: rgba(255,255,255,.78);
  backdrop-filter: blur(14px);
  box-shadow: var(--shadow);
}
.modal__dialog--wide{width:min(980px, calc(100% - 28px))}
.modal__close{
  position:absolute; right:12px; top:12px;
  width:40px; height:40px;
  border-radius: 16px;
  border:1px solid var(--line);
  background: rgba(255,255,255,.70);
  cursor:pointer;
}
.product-modal{display:grid; grid-template-columns: 1fr 1fr; gap:16px; align-items:start}
.product-modal__img{width:100%; border-radius: var(--radius); border:1px solid var(--line); aspect-ratio: 4/3; object-fit:cover}
.product-modal__price{font-weight:800; font-size:18px; margin: 10px 0 12px}
.product-modal__bullets{display:grid; gap:8px; margin: 12px 0 16px}
.bullet{display:flex; gap:10px; align-items:flex-start}
.bullet span{margin-top:2px}

/* Reveal animation */
.reveal{
  opacity:0;
  transform: translateY(14px);
  transition: opacity .7s ease, transform .7s ease;
  will-change: transform, opacity;
}
.reveal.is-in{
  opacity:1;
  transform: translateY(0);
}

/* Responsive */
@media (max-width: 980px){
  .cards{grid-template-columns: repeat(2, 1fr)}
  .catalog{grid-template-columns: repeat(2, 1fr)}
  .grid4{grid-template-columns: repeat(2, 1fr)}
  .hero__grid{grid-template-columns: 1fr}
  .split{grid-template-columns: 1fr}
  .product-modal{grid-template-columns: 1fr}
}
@media (max-width: 720px){
  .nav{display:none}
  .burger{display:inline-grid; place-items:center}
  .header__actions .btn--ghost{display:none}
  .contact-grid{grid-template-columns: 1fr}
}
@keyframes gradientFlow {
  0%   { background-position: 0% 50%; }
  50%  { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}
@keyframes floatGlow{
  0%   { transform: translateY(-4%) translateX(-3%); }
  100% { transform: translateY(4%) translateX(3%); }
}

</style>
<script>
(() => {
  const qs = (s, p = document) => p.querySelector(s);
  const qsa = (s, p = document) => [...p.querySelectorAll(s)];

  // Mobile burger: simply toggles nav visibility by adding a class on body
  const burger = qs("[data-burger]");
  if (burger) {
    burger.addEventListener("click", () => {
      document.body.classList.toggle("is-menu");
      const nav = qs(".nav");
      if (nav) nav.style.display = document.body.classList.contains("is-menu") ? "flex" : "";
    });
  }

  // Reveal on scroll
  const reveals = qsa(".reveal");
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) e.target.classList.add("is-in");
    });
  }, { threshold: 0.12 });
  reveals.forEach(el => io.observe(el));

  // Floating CTA shows after scroll a bit (conversion)
  const floating = qs("[data-floating-cta]");
  if (floating) {
    const onScroll = () => {
      const y = window.scrollY || document.documentElement.scrollTop;
      floating.classList.toggle("is-visible", y > 520);
    };
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
  }

  // Modals
  const openModal = (name) => {
    const m = qs(`[data-modal="${name}"]`);
    if (!m) return;
    m.classList.add("is-open");
    m.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  };

  const closeModal = (modalEl) => {
    modalEl.classList.remove("is-open");
    modalEl.setAttribute("aria-hidden", "true");
    document.body.style.overflow = "";
  };

  qsa("[data-open-modal]").forEach(btn => {
    btn.addEventListener("click", () => openModal(btn.getAttribute("data-open-modal")));
  });

  qsa("[data-modal]").forEach(modal => {
    qsa("[data-close-modal]", modal).forEach(el => {
      el.addEventListener("click", () => closeModal(modal));
    });
  });

  window.addEventListener("keydown", (e) => {
    if (e.key !== "Escape") return;
    const open = qs(".modal.is-open");
    if (open) closeModal(open);
  });

  // Catalog filters
  const chips = qsa("[data-filter]");
  const products = qsa(".product");
  if (chips.length && products.length) {
    chips.forEach(ch => {
      ch.addEventListener("click", () => {
        chips.forEach(c => c.classList.remove("is-active"));
        ch.classList.add("is-active");

        const f = ch.getAttribute("data-filter");
        products.forEach(p => {
          const tags = (p.getAttribute("data-tags") || "").split(" ");
          const show = (f === "all") || tags.includes(f);
          p.style.display = show ? "" : "none";
        });
      });
    });
  }

  // Product modal content
  qsa("[data-product]").forEach(btn => {
    btn.addEventListener("click", () => {
      const title = btn.getAttribute("data-title") || "";
      const desc = btn.getAttribute("data-desc") || "";
      const price = btn.getAttribute("data-price") || "";
      const img = btn.getAttribute("data-img") || "";

      const modal = qs('[data-modal="product"]');
      if (!modal) return;

      const t = qs("[data-product-title]", modal);
      const d = qs("[data-product-desc]", modal);
      const p = qs("[data-product-price]", modal);
      const i = qs("[data-product-img]", modal);

      if (t) t.textContent = title;
      if (d) d.textContent = desc;
      if (p) p.textContent = price;
      if (i) { i.src = img; i.alt = title; }

      openModal("product");
    });
  });

  // Fake submit (replace with your backend / telegram bot / email service)
  qsa("[data-form]").forEach(form => {
    form.addEventListener("submit", (e) => {
      e.preventDefault();
      const btn = qs('button[type="submit"]', form);
      if (btn) {
        const old = btn.textContent;
        btn.textContent = "Отправлено ✓";
        btn.disabled = true;
        setTimeout(() => {
          btn.textContent = old;
          btn.disabled = false;
          form.reset();
        }, 1600);
      }
    });
  });
})();
</script>
</body>